<div class="modal fade" id="group{{ group.id }}" role="dialogue">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
        <h4 class="modal-title">
          {{ group is empty ? 'Create Team' : 'Edit ' ~ group.name }}
        </h4>
      </div>
      {% if group %}
      <form action="{{ path_for('group-post', { 'id': group.id }) }}" method="post">
      {% else %}
      <form action="{{ path_for('group-post') }}" method="post">
      {% endif %}

        <div class="modal-body">

          <div class="form-group">
            <label for="name">Name:</label>
            <input class="form-control" name="name" id="name" type="text" value="{{ group.name }}" placeholder="Enter name" />
          </div>

          <div class="form-group">
            <label for="description">Description: (supports <a target="_blank" href="https://github.com/adam-p/markdown-here/wiki/Markdown-Cheatsheet">Markdown</a>)</label>
            <textarea class="form-control" name="description" id="description">{{ group.description }}</textarea>
          </div>

        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Close</button>
          <button class="btn btn-primary">Save</button>
        </div>
      </form>
    </div>
  </div>
</div>